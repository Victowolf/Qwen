apiVersion: v1
kind: Service
metadata:
  name: qwen-lora-service
spec:
  type: NodePort
  selector:
    job-name: qwen-lora-server   # 🔥 MUST match your Job metadata.name
  ports:
    - name: http
      port: 8000
      targetPort: 8000
      nodePort: 30124